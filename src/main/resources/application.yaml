spring:
  cache:
    jcache:
      config: classpath:ehcache.xml

server:
  shutdown: graceful

management:
  server:
    port: 9000
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

springdoc:
  use-fqn: true
  swagger-ui:
    enabled: false
  api-docs:
    path: api-docs

swaply:
  client:
    cnb:
      url: https://www.cnb.cz
      connection-timeout: 3000
      read-timeout: 3000
